# Objective: Single consul + nomad + vault(?) server
#   with no HA; just to demo
#   can have multiple workers with different chracteristics
#   sf1: single foundation 1 (consul + nomad + vault)
#   sw1: DB node1 (system), demo service1
#   sw2: DB node2 (system), demo service2, demo scheduler1
#   sw3: DB node3 (system), demo functions1